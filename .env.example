# ESPN Fantasy Football private league credentials.
# Copy this file to `.env` and fill in real values. Never commit `.env`.

# The numeric ID of your ESPN fantasy football league (found in the league URL).
LEAGUE_ID=

# ESPN session cookies used to authenticate against private leagues.
# See README.md for instructions on retrieving these from your browser.
ESPN_S2=
SWID=

# Optional: current season year. Defaults to the current calendar year if unset.
CURRENT_SEASON=

# Optional: enables Claude-generated weekly recap commentary.
# If unset, the app falls back to deterministic placeholder commentary.
ANTHROPIC_API_KEY=

# Optional: licensed FantasyPros API key (api.fantasypros.com), used for
# rest-of-season rankings in Roster Strength. Not required to run the app.
FANTASYPROS_API_KEY=

# Optional: enables the "Ask Me Anything" page (Gemini-powered natural-
# language Q&A over league history/rosters). Without it, that page shows
# a setup message instead of crashing. Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: override the Gemini model used for Ask Me Anything. Defaults
# to gemini-2.5-flash if unset - check https://ai.google.dev/gemini-api/docs/models
# for the current recommended Flash model before changing this.
GEMINI_MODEL=

# Optional/legacy: a shared password required to use the whole app - a
# simple backup gate, superseded by real per-user Google login below.
# Leave unset locally - the app skips the password prompt entirely when
# this is empty.
APP_PASSWORD=

# Real per-user login (Google, via Streamlit's native st.login()) - the
# OAuth client itself is configured separately in .streamlit/secrets.toml
# (see README's "Email login" setup section), NOT here. These two just
# control who's let in *after* a successful Google sign-in:

# Comma-separated allowlist of Google account emails allowed to use the
# site at all. Empty by default - fails CLOSED (nobody but ADMIN_EMAIL
# gets in) rather than accidentally allowing any Google account.
ALLOWED_EMAILS=

# The one email that can see the commissioner-only War Room page. Should
# also be included in ALLOWED_EMAILS above.
ADMIN_EMAIL=

# Optional: enables scheduled GroupMe posts (matchup slate updates,
# weekly waiver recap - see scripts/post_*.py and .github/workflows/).
# Create a Bot for your group at https://dev.groupme.com/bots, then paste
# its bot_id here (and as a GitHub Actions secret of the same name).
GROUPME_BOT_ID=

# Optional: a SEPARATE bot bound to a private group containing only the
# commissioner - used for the Tuesday waiver-recommendations
# notification instead of the shared league bot above. Create a second
# bot at https://dev.groupme.com/bots pointed at that private group.
GROUPME_PERSONAL_BOT_ID=

# Optional: a GitHub Personal Access Token with `repo` write access to
# THIS repo - used only by War Room's Lineup Optimizer "upload weekly
# rankings PDF" feature to commit the parsed override so it (a) survives
# a Streamlit Cloud disk wipe and (b) is visible to the Wednesday/Sunday
# GitHub Actions scripts (a separate environment that only ever sees
# what's actually committed to the repo). This is the one secret in this
# project that flows the OPPOSITE direction from every other one here -
# used FROM the deployed app TO push to GitHub - so it must be set as a
# Streamlit Cloud secret, NOT a GitHub Actions secret (Actions runs never
# call this). Create one at https://github.com/settings/tokens with
# just the `repo` scope. Without it, an uploaded override still applies
# for the current app session, it just won't persist past a redeploy or
# reach the scheduled scripts.
GITHUB_TOKEN=

# Optional: owner/repo the override above commits to. Defaults to
# mattklei1/fantasy-football if unset.
GITHUB_REPO=
